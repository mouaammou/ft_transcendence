"use client";

const Layout = ({ children }) => {

	// const { isAuthenticated } = useAuth();

	// if (!isAuthenticated) {
	// 	return null;
	// } else
		return <>{children}</>;
};

export default Layout;
